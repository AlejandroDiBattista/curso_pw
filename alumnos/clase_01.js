const CentimetrosPorPulgadas = 2.54;

let pulgadas = 50;
let centimetros = pulgadas * CentimetrosPorPulgadas;

let mensaje = `- ${pulgadas} in. es igual a ${centimetros} cm.`;

console.log('Ejemplo de un programa para convertir pulgadas a centímetros v.2')
console.log(mensaje);
